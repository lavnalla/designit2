fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
# Core ML deps are shared with fashion-segmentation-test/.venv
# Install full stack with:
#   pip install -r ../fashion-segmentation-test/requirements.txt -r requirements.txt
